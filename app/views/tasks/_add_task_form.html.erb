<%= form_for(@task, remote: true)  do |f| %>
  <%= f.text_field :name, class: 'form-control today-task-field' %> 
  <%= f.hidden_field :date, :value => Date.today %>
  <br>
  <%= f.submit "Create task", class: "btn btn-success btn-sm cancel" %>
<% end %>