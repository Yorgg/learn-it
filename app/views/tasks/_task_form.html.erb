 
<script>
  jQuery(function() {
  var tz = jstz.determine();
  $.cookie('timezone', tz.name(), { path: '/' });
  }); 
 </script>

 <%= form_for(@task) do |f| %>
  <%= f.label :Day %>
  <%= f.date_field :date, class: 'form-control' %> 
  
  <%= f.label :Name %>
  <%= f.text_field :name, class: 'form-control' %> 
  
  <br>
  <%= f.submit "Create task", class: "btn btn-success btn-block" %>
<% end %>

