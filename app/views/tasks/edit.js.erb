var currentElement = $('#task-container-' + <%= @task.id %>);
currentElement.find('.to-do').html("<%= @task.name %>");
currentElement.find('.edit-task-form').hide();
currentElement.find('.all-list-data').toggle();

 