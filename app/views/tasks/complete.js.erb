var taskId = <%= @task.id %>;
$('#task-container-' + taskId).find('.to-do').toggleClass('complete');
$('#task-container-' + taskId).find('.check-box').toggleClass('glyphicon-check glyphicon-unchecked');

